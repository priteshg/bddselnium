if (options.waitForResults) {
  await expect.poll(
    async () =>
      (await this.frame.getByTestId('message-header').count()) > 0 ||
      (await this.frame.getByTestId('empty-state-sub-title').count()) > 0,
    { timeout: 15000 }
  ).toBe(true);
}